apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

# values.yaml n'est pas listé ici : il est consommé par le chart Helm via
# $values, pas rendu comme manifeste.
resources:
  - tlsstore.yaml
