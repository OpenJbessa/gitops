apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

# roles.yaml est absent d'ici volontairement : ce sont des ressources Teleport
# appliquées avec tctl, pas des objets Kubernetes (voir l'en-tête du fichier).
# values.yaml est consommé par le chart via $values.
resources:
  - ingressroute-tcp.yaml
  - recordings-gc.yaml
